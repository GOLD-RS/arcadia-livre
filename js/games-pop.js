/* Bubble/arcade game module. Behavior intentionally mirrors the legacy factory. */
(function(){window.ArcadiaGameModules=window.ArcadiaGameModules||{};window.ArcadiaGameModules.pop=function(env){const {root,title,recordText,saveRecord,addRestartButton,registerCleanup,lifecycle}=env;const isPaused=()=>lifecycle?.state!=='running';return function pop(){title.textContent='Bubble Pop';root.innerHTML=`<div class="game-ui classic-game"><h3>Estoure as bolhas</h3><p>Tempo: <span class="timer">20</span>s · pontos: <span class="score">0</span>${recordText('pop')}</p><canvas width="320" height="240" role="img" aria-label="Área de bolhas do Bubble Pop"></canvas></div>`;const canvas=root.querySelector('canvas'),ctx=canvas.getContext('2d');let bubbles=[],score=0,time=20,started=false,over=false;function make(){bubbles=Array.from({length:13},()=>({x:18+Math.random()*284,y:20+Math.random()*195,r:10+Math.random()*12,hue:Math.floor(Math.random()*360)}))}function draw(){ctx.fillStyle='#080e20';ctx.fillRect(0,0,320,240);bubbles.forEach(b=>{ctx.fillStyle=`hsl(${b.hue} 90% 65%)`;ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill();ctx.fillStyle='#ffffff88';ctx.beginPath();ctx.arc(b.x-b.r/3,b.y-b.r/3,b.r/4,0,Math.PI*2);ctx.fill()})}function finish(){over=true;saveRecord('pop',score);root.querySelector('h3').textContent=`Fim! ${score} bolhas.`;addRestartButton()}function tap(e){if(over)return;if(!started)started=true;const r=canvas.getBoundingClientRect(),x=(e.clientX-r.left)*canvas.width/r.width,y=(e.clientY-r.top)*canvas.height/r.height;const i=bubbles.findIndex(b=>Math.hypot(x-b.x,y-b.y)<b.r);if(i>=0){bubbles.splice(i,1);score++;root.querySelector('.score').textContent=score;if(!bubbles.length)make()}draw()}canvas.addEventListener('pointerdown',tap);const tick=setInterval(()=>{if(isPaused()||!started||over)return;time--;root.querySelector('.timer').textContent=time;if(time<=0)finish()},1000);make();draw();registerCleanup(()=>{clearInterval(tick);canvas.removeEventListener('pointerdown',tap);root.innerHTML=''})}};})();
