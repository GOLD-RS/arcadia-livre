/* Migrated color game module. */
(function(){window.ArcadiaGameModules=window.ArcadiaGameModules||{};window.ArcadiaGameModules.color=function(env){const {root,title,recordText,saveRecord,addRestartButton,registerCleanup,holdButton,lifecycle}=env;const isPaused=()=>lifecycle?.state!=='running';return function color(){title.textContent='Color Circuit';root.innerHTML=`<div class="game-ui classic-game"><h3>Combine as cores</h3><p>Tempo: <span class="timer">20</span>s · pontos: <span class="score">0</span>${recordText('color')}</p><div class="color-target">?</div><div class="color-buttons"><button data-color="pink">ROSA</button><button data-color="cyan">CIANO</button><button data-color="lime">LIMA</button><button data-color="yellow">AMARELO</button></div></div>`;const colors=['pink','cyan','lime','yellow'],target=root.querySelector('.color-target');let wanted='',score=0,time=20,started=false,over=false;function next(){wanted=colors[Math.floor(Math.random()*4)];target.textContent=wanted==='pink'?'ROSA':wanted==='cyan'?'CIANO':wanted==='lime'?'LIMA':'AMARELO';target.style.color=`var(--${wanted})`}function answer(e){if(over)return;if(!started)started=true;if(e.currentTarget.dataset.color===wanted){score++;root.querySelector('.score').textContent=score;next()}}root.querySelectorAll('[data-color]').forEach(b=>b.addEventListener('click',answer));const tick=setInterval(()=>{if(isPaused()||!started||over)return;time--;root.querySelector('.timer').textContent=time;if(time<=0){over=true;saveRecord('color',score);root.querySelector('h3').textContent=`Fim! ${score} pontos.`;addRestartButton()}},1000);next();registerCleanup(()=>clearInterval(tick))}};})();
